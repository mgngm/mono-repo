version: 1
applications:
  - appRoot: nextjs-amplified-app/
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      cache:
        paths:
          - node_modules/**/*