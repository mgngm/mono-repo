version: 1
applications:
  - appRoot: front-end/
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*